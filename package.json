{
  "name": "world-countries-cities-db",
  "version": "1.0.0",
  "description": "Worldwide countries, states and cities database (JSON, CSV, SQL, YAML), split by country/region/subregion/bundle, bilingual EN/ES, plus a ready-to-use LATAM set.",
  "private": true,
  "license": "MIT",
  "//license": "MIT applies to the code in /scripts (see LICENSE-CODE). The DATA is under ODbL v1.0 (see LICENSE).",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node --max-old-space-size=4096 scripts/build.js",
    "build:latam": "node scripts/build-latam.js",
    "build:all": "npm run build && npm run build:latam",
    "validate": "node scripts/validate.js"
  },
  "keywords": [
    "countries",
    "states",
    "cities",
    "cities-database",
    "countries-database",
    "country-state-city",
    "world-cities",
    "json",
    "csv",
    "sql",
    "yaml",
    "dataset",
    "geodata",
    "latam"
  ]
}
